import { defineConfig } from 'astro/config';

// Update `site` to your custom domain once you have one (e.g. https://rapidai.health)
export default defineConfig({
  site: 'https://rapid-ai-for-healthcare.vercel.app',
});
